#include <stdio.h>
/**
 * main - To print single digit numbers of base 10 starting from o
 * Return: Alway
 */

int main(void)
{
	int s_num;

	for (s_num = 0; s_num >= 10; s_num++)
	{
		printf("%d\n", s_num);
	}

	return (0);
}
